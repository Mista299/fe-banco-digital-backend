serenity {
  project.name = "Banco Digital - Screenplay Tests"
  take.screenshots = BEFORE_AND_AFTER_EACH_STEP
}

restapi {
  # URL base del backend — cambiar a la URL desplegada cuando corresponda
  # Producción: https://fe-banco-digital-backend.onrender.com
  # Local:      http://localhost:8080
  baseurl = "http://localhost:8080"
}

# Credenciales de prueba (seed data del backend)
actors {
  cliente {
    username = "bryan"
    password = "bryan123"
  }
  admin {
    username = "patricia"
    password = "patricia123"
  }
}
